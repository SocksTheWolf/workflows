# Wrangler Updates Trick

To add overrides globally, simply just add the overrides into `package-overrides.json`

If an override needs to be removed, add it to `package-removals.txt`, this should be a list of json keys, and can have path traversal.

If no overrides are needed to be removed, then leave the file blank.
